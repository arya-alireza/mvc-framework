{% extends "layouts/app.twig" %}
{% block main %}
    <h2>{{ hello }}</h2>
    {% if auth() %}
    <a href="{{ route('dashboard') }}">Dashboard</a>
    {% else %}
    <a href="{{ route('login') }}">Login</a>
    {% endif %}
{% endblock %}